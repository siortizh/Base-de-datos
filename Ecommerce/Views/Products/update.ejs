<%- include("../header"); %>
<div class="col-12">
    <h1>Edit product</h1>
    <form method="post" action="/products/update">

        <input value="<%= product.id %>" name="id" type="hidden">
        <div class="form-group">
            <label for="name">Name</label>
            <input value="<%= product.name %>" required id="name" placeholder="Product name"
                class="form-control" type="text" name="name">
        </div>
        <div class="form-group">
            <label for="price">Price</label>
            <input value="<%= product.price %>" required id="price" placeholder="Poduct price"
                class="form-control" type="number" name="price">
        </div>
        <div class="form-group">
            <button class="btn btn-success">Guardar</button>
            &nbsp;<a href="/products" class="btn btn-primary">Volver</a>
        </div>
    </form>
</div>
<%- include("../footer"); %>